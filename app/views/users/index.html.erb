<h1>All Users</h1>
<% @users.each do |user| %>
  <div>
    <img src="<%= user.photo %>"/>
    <div>
      <h2><%= link_to user.name, user %></h2>
      <p>Number of Posts:
        <%= user.posts.count %></p>
    </div>
  </div>
<% end %>
